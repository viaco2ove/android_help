[project]
name = "android-help"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
adb-clipboard = "adb_tools.clipboard.clipboard:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
